def ev(c):#funcion de ev(c)
    v="aeiouAEIOU"#añade a v el vaor de las vocales
    return c in v#regresa "x" en "v"
c=input("Introduce un carácter: ")#hace que puedas poner valor
if ev(c):#si "ev" esta en "c" se realiza
    print(f"El carácter '{c}' es una vocal.")#coloca el resultado
else:#si no se cumple se realiza
    print(f"El carácter '{c}' no es una vocal.")#coloca el resultd
